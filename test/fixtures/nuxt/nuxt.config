// https://nuxt.com/docs/api/configuration/nuxt-config
export default defineNuxtConfig({

  modules: ['@nuxt/ui', '@nuxt/eslint'],
  ssr: false,
  devtools: { enabled: true },
  app: {
    head: {
      title: '数字余华',
      meta: [
        { name: 'description', content: '一个基于AI的余华风格对话机器人' },
        { name: 'viewport', content: 'width=device-width, initial-scale=1, viewport-fit=cover' },
      ],
      link: [
        { rel: 'icon', type: 'image/x-icon', href: '/yuhuashuwu.ico' },
      ],
    },
  },
  css: ['~/assets/css/main.css', '~/assets/css/input-policy.css'],
  ui: {
    fonts: false,
  },
  runtimeConfig: {
    public: {
      // Expose env to client with NUXT_PUBLIC_* naming
      chatToken: process.env.NUXT_PUBLIC_CHAT_TOKEN || '',
      chatBotId: process.env.NUXT_PUBLIC_CHAT_BOT_ID || '',
      chatBaseWsURL: process.env.NUXT_PUBLIC_CHAT_BASE_WS_URL || '',
      chatVoiceId: process.env.NUXT_PUBLIC_CHAT_VOICE_ID || '',
    },
  },
  devServer: {
    port: 3000, // Replace with your desired port
    host: '0.0.0.0', // Optional: use '0.0.0.0' to make it accessible on your local network
  },
  compatibilityDate: '2025-07-15',
  // Cloudflare Workers preset (set to 'cloudflare' for Workers or 'cloudflare-pages' for Pages)
  nitro: {
    preset: process.env.NITRO_PRESET || 'cloudflare-pages',
  },
  vite: {
    logLevel: 'info',
    clearScreen: false,
    server: {
      allowedHosts: true,
    },
  },
  eslint: {
    config: {
      stylistic: true, // 让@nuxt/eslint 这个module开启美化模式
      standalone: false, // 为了让两个config可以共存. 详见官方文档
    },
  },
})
