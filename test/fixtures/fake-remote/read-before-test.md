RClone supports creating an `alias-type` remote, which we can utilize to set a local folder as a fake remote. By doing this, we can test the whole process of the sync action.


In our case, we create a remote name `fake-remote` and point it to `replace-with-root-path/test/fixtures/fake-remote`

